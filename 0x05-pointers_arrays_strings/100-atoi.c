#include "main.h"

/**
 * _atoi - a function that convert a string to an integer.
 * @s: string to be converted to an integer
 * Return: 0 if there no number in the string.
 */
int _atoi(char *s)
{
	int i = 0;
	int j = 0;
	int k = 0;
	int l = 0;
	int len = 0;


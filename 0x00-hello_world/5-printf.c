#include <stdio.h>

/**
 * main - Entry point print with printf function
 * Return: zero if successful but non-zero if failed
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

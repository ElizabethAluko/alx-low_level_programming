#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: 0 if successful
 */
int mul(int a, int b)
{
	int prd;

	prd = a * b;
	return (prd);
}

#include "main.h"

/**
 * mod - find the modulus of two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: the modulus of the two numbers.
 */
int mod(int a, int b)
{
	return (a % b);
}
